import React from 'react';
import './tool-feature.css';

export const ToolFeature = ({feature}) => {
    return (
        <span className='tool-feature'>
            {feature}
        </span>
    );
};